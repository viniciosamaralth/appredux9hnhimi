import { colors } from "@material-ui/core";

export default {
  primary: {
    main: colors.red["800"],
    dark: colors.red["900"],
    light: colors.red["700"],
  },
  secondary: {
    main: colors.red["800"],
    dark: colors.red["900"],
    light: colors.red["700"],
  },
};
